{
  "name": "csc-308-app",
  "version": "1.0.0",
  "description": "First app for cs-308",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run dev --prefix packages/react-frontend",
    "frontend": "npm run dev --prefix packages/express-backend"
  },
  "workspaces": [
    "packages/react-frontend"
  ],
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
